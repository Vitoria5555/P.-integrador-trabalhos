
1) print = ("nome", "idade" ,"cidade")
nome = input ( " entre com o seu nome: ")

print = ( "nome", "idade", "cidade")

nome, idade, cidade = input ( " ente com o seu nome, idade e cidade: ")

resultado:
entre com o seu nome: vitória
 ente com o seu nome, idade e cidade: vitória - idade 22 , cidade - Rio de Janeiro 
2) a = float(input("Primeiro número:"))
b = float(input("Segundo número:"))
operação = input("Digite a operação a realizar (+,-,* ou /):")
if operação == "+":
    resultado = a + b
elif operação == "-":
    resultado = a - b
resultado:
Primeiro número:1
Segundo número:2
Digite a operação a realizar (+,-,* ou /):1+2
1+2
3
1-2
-1
1/2
0.5

3) ef imprimir_itens_lista():
    # Solicita ao usuário que digite os itens da lista de compras separados por vírgula
    lista_compras = input("Digite os itens da lista de compras separados por vírgula: ")

    # Divide a entrada do usuário em uma lista de itens
    itens = lista_compras.split(',')

    # Imprime os itens separadamente usando um laço
    print("Itens da lista de compras:")
    for item in itens:
        print(item.strip())  # strip() remove espaços em branco extras
resultado:
Digite os itens da lista de compras separados por vírgula: ovo,banana,leite
Itens da lista de compras:
item 1: ovo
item 2: banana
item 3: leite
4) celsius = float(input("Digite a temperatura em graus Celsius: "))

    # Converte a temperatura para Fahrenheit usando a fórmula de conversão
fahrenheit = (celsius * 9/5) + 32

    # Imprime o resultado
print("A temperatura em Fahrenheit é:", fahrenheit)

resultado: 
Digite a temperatura em graus Celsius: 7
A temperatura em Fahrenheit é: 44.6

5)
def solicitar_nomes():
    # Lista para armazenar os nomes digitados
    nomes = []

    while True:
        # Solicita ao usuário que digite um nome
        nome = input("Digite um nome (ou 'sair' para sair): ")

        # Verifica se o nome digitado é 'sair'
        if nome.lower() == 'sair':
            break  # Sai do loop se 'sair' for digitado
        else:
            nomes.append(nome)  # Adiciona o nome à lista

    # Imprime todos os nomes digitados
    print("Nomes digitados:")
    for nome in nomes:
        print(nome)
resultado:
Digite um nome (ou 'sair' para sair): ana, maria, joana
Digite um nome (ou 'sair' para sair): ana
Digite um nome (ou 'sair' para sair): maria
Digite um nome (ou 'sair' para sair): joana
Digite um nome (ou 'sair' para sair): sair
Nomes digitados:
ana, maria, joana
ana
maria
joana





